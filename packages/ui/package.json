{
  "name": "ui",
  "version": "1.0.0",
  "private": true,
  "description": "Shared UI components for the monorepo",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'UI package - no build step needed (consumed directly)'",
    "test": "echo \"No tests yet\" && exit 0",
    "lint": "eslint src --ext .ts,.tsx",
    "clean": "rm -rf node_modules"
  },
  "peerDependencies": {
    "react": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "eslint": "^8.57.0",
    "typescript": "^5.5.0"
  }
}
