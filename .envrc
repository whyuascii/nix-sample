# direnv configuration for automatic Nix shell activation
# direnv watches this file and automatically loads the Nix environment when you cd into this directory

# Set a longer warning timeout for slow operations
export DIRENV_WARN_TIMEOUT=20s

# Load the default Nix development shell from flake.nix
# --impure flag allows access to environment variables like PWD (needed for devenv)
use flake . --impure

# After running 'direnv allow', you'll automatically have:
# - Node.js 22
# - pnpm, yarn, npm (via corepack)
# - git, jq, skopeo
# - All environment variables defined in flake.nix

# For app-specific shells, create .envrc files in apps/web and apps/api with:
#   use flake ../..#dev-web --impure
#   use flake ../..#dev-api --impure
